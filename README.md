# f3bypass
f3bypass automates common access-control bypass techniques used during web application security assessments.  The tool helps identify authorization weaknesses caused by misconfigured web servers, reverse proxies, load balancers, WAFs, and access-control implementations by testing multiple request variations against a protected resource.
